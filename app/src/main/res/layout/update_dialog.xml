<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/edt_updated_name"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:hint="Ažurirano Ime"/>

    <EditText
        android:id="@+id/edt_updated_type"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:hint="Ažurirani Tip"/>

    <EditText
        android:id="@+id/edt_updated_date"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:hint="Ažurirani Datum"/>

    <EditText
        android:id="@+id/edt_updated_img_url"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:hint="Ažurirana Slika"/>

    <EditText
        android:id="@+id/edt_updated_state"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:hint="Ažurirano Stanje (Dobro/Loše)"/>

</LinearLayout>
